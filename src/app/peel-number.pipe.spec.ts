import { PeelNumberPipe } from './peel-number.pipe';

describe('PeelNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PeelNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
